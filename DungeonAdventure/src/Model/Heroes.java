package Model;

public abstract class Heroes extends DungeonCharacter {

    // still need to change not done
    protected Heroes(int theHp, String theChName, int theRange, int theAttackSpeed, int theHitChance) {
        super(theHp, theChName, theRange, theAttackSpeed, theHitChance);
    }
}
